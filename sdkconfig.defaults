# HB-RF-ETH Default Configuration
# Note: SNMP defines (CONFIG_LWIP_SNMP, CONFIG_LWIP_SNMP_MAX_OBJ_ID_LEN,
# CONFIG_LWIP_MIB2_CALLBACKS) are set via build_flags in platformio.ini.
# HTTPD max_uri_handlers is configured programmatically in webui.cpp.
