/**
 * Central configuration for the external documentation / wiki link rendered
 * in the sidebar (Korrekturauftrag §8). Hard-coded exactly once here so a
 * future change touches a single line instead of being scattered through the
 * header template + mobile panel.
 *
 * The link opens in a new tab (target="_blank" rel="noopener noreferrer") and
 * is rendered with an external-link icon in NewDesignHeader.vue.
 */
export const DOCUMENTATION_URL = 'https://github.com/Xerolux/HB-RF-ETH-ng/wiki'
