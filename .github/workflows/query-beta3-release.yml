name: Query Recovery Hotfix Status

on:
  push:
    branches:
      - ops/beta3-release-status

permissions:
  actions: read
  contents: write

jobs:
  query:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout status branch
        uses: actions/checkout@v7
        with:
          ref: ops/beta3-release-status
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Query hotfix workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh run list \
            --repo "${{ github.repository }}" \
            --workflow apply-recovery-hotfix.yml \
            --limit 10 \
            --json databaseId,status,conclusion,createdAt,displayTitle,event,headSha,url \
            > recovery-hotfix-status.json

      - name: Commit status report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add recovery-hotfix-status.json
          git commit -m "ci: record recovery hotfix status"
          git push origin HEAD:ops/beta3-release-status
